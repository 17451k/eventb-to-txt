language: python

dist: bionic

python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

before_script:
  - pip install coveralls pytest

install:
  - pip install .

script:
  - coverage run --source eventb_to_txt -m pytest

after_success:
  - coveralls
